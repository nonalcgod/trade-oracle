# Railway Deployment - Core API Dependencies Only
# Last Updated: 2025-11-05 - TESTED COMPATIBLE VERSIONS
# (scipy/numpy excluded - only needed for local backtesting)

# FastAPI Backend
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.18

# Market Data & Trading
# CRITICAL: alpaca-py 0.30.x has httpx<0.26 constraint
# Updated to 0.35.0 which supports httpx>=0.26 (compatible with supabase)
alpaca-py==0.35.0

# Database
# DOWNGRADE: 2.23.0 has proxy parameter bug with httpx 0.27.2
# Using 2.15.1 (mentioned in Discussion #35608 as working version for proxy bug)
supabase==2.15.1

# Cache
# REMOVED redis==5.0.1 - conflicts with upstash-redis 0.15.0
# upstash-redis includes redis client, don't need both
upstash-redis==1.0.0

# AI/LLM (optional for weekly reflections)
anthropic==0.39.0

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
pytz==2023.3

# HTTP Clients
# NOTE: httpx version controlled by supabase - do not pin explicitly
websockets==12.0

# Logging & Monitoring
structlog==24.4.0
