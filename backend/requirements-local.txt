# Local Development Dependencies
# Synced with requirements-railway.txt (2025-11-05)
# Includes additional packages for backtesting and testing

# FastAPI Backend (matched with Railway)
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.18

# Market Data & Trading (matched with Railway)
alpaca-py==0.35.0

# Database (matched with Railway)
supabase==2.15.1

# Cache (matched with Railway)
upstash-redis==1.0.0

# AI/LLM (matched with Railway)
anthropic==0.39.0

# Utilities (matched with Railway)
python-dotenv==1.0.0
python-dateutil==2.8.2
pytz==2023.3

# HTTP Clients (matched with Railway)
websockets==12.0

# Logging & Monitoring (matched with Railway)
structlog==24.4.0

# Math & Scientific Computing (local development only - for backtesting)
numpy==1.26.2
scipy==1.11.4
pandas==2.1.3

# Testing (local development only)
pytest==8.4.2
pytest-asyncio==0.24.0
pytest-mock==3.15.1
